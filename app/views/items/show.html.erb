<body style="min-height: 100vh;">

<div class="container">
    <div class="row justify-content-center text-center">
        <div class="col-md-6">
            <div class="text-center card border-dark" style="margin: 50px;">

            <h1><%= @item.title%></h1>
            <p class="p-2"><%= image_tag(@item.photo, class: "image_1", style: "max-width:100%;") %></p>
            <p><%= @item.description%></p>
            <h4>Prix: <%= @item.price%> €</h4>
            <%= button_to 'Ajouter au panier', add_to_cart_item_path(@item), method: :post, class: 'btn btn-success m-2' %>
            </div>
        </div>
    </div>
</div>
</body>
